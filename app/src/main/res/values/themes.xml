<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- App XML theme (Compose still sets its own theme in code) -->
    <style name="Theme.SenseWay" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:forceDarkAllowed">true</item>
        <item name="android:windowFullscreen">false</item>
    </style>
</resources>
